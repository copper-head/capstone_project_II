# fn-6-m69.1 Schema & model updates: add existing_event_id, migrate sentinel→Optional

## Description
Add `existing_event_id: Optional[int] = None` to both `ExtractedEvent` and `LLMResponseEvent` models. Migrate `LLMResponseEvent` from all-required-string-with-"none"-sentinels to proper `Optional[str] = None` fields. Update `_convert_event()` in `llm.py` to remove sentinel→None conversion (now handled natively). Update all existing tests that construct these models.

**Files to modify:**
- `src/cal_ai/models/extraction.py` — Add `existing_event_id` field to `ExtractedEvent` (line ~30-58) and `LLMResponseEvent` (line ~164-191). Change `LLMResponseEvent` optional fields from `str` with "none" default to `Optional[str] = None`.
- `src/cal_ai/llm.py` — Simplify `_convert_event()` (line ~244-284) to remove "none"→None conversion logic.
- `tests/unit/test_models.py` — Update model construction tests.
- `tests/unit/llm/test_gemini.py` — Update conversion tests.

**Key decisions:**
- `existing_event_id` is `Optional[int]` (not UUID string) because ID remapping (task 2) maps UUIDs→integers for the LLM.
- The integer maps back to a real Google Calendar event ID via the reverse map built in task 2.
- Keep `action` field as `Literal["create", "update", "delete"]` — no changes needed.

**Acceptance:**
- [ ] `ExtractedEvent` has `existing_event_id: Optional[int] = None`
- [ ] `LLMResponseEvent` uses `Optional[str] = None` instead of `str` with "none" sentinel
- [ ] `_convert_event()` simplified — no sentinel conversion needed
- [ ] All existing tests pass
- [ ] New tests for `existing_event_id` field (present and absent)
- [ ] ruff clean
## Acceptance

## Done summary
Added existing_event_id (Optional[int]) to ExtractedEvent, ValidatedEvent, and LLMResponseEvent. Migrated LLMResponseEvent from "none" sentinel strings to proper Optional fields, simplified _convert_event() to remove sentinel conversion, and updated system prompt and all tests accordingly.
## Evidence
- Commits: b34c3637251d427a7a6100ada5ec4f56a54517f2
- Tests: python3 -m pytest --tb=short -q (264 passed), python3 -m ruff check . (all checks passed)
- PRs: