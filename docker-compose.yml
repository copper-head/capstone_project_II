services:
  cal-ai:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./credentials.json:/app/credentials.json:ro
      - ./token.json:/app/token.json
      - ./samples:/app/samples:ro
      - ./data:/app/data
